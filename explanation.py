'''exersise 2.3'''
# i have created a file with echo > goodbye.py then in my text editor i added goodbye function that do goodbye {name} then i did git add goodbye.py and then did commit that added file in tracking
# then i ran git log --oneline that gave me two commits text one for adding greeting.py file and then adding goodbye.py file
# then i changed greet.py file and did git diff which told which thing i changed and what is added (+) and what was removed (-)
# then i made two files (file_a.py, and file_b.py) and did git add . which added all two files which were in the directory
# git log only tells a lengthy details of commited statements which is essential because in companies  the person which commit something is resposible for the change so every detail is mention like name and mail and at what time a person commit the file
 
 
'''exersise 3.3'''
# when i changed goodbye.py then i did git restore goodbye.py which fully restore the file at the point where it was last commit
# when i did git status after restore then it says branch tree is clean no change
# when we try to do restore on a file that is not change it doesnt show anything in mac terminal.
# when we are working in a team and if other person commit a function in the file and that function is used by many other function, if we remove the bugged function the other code will not be worked so thats why we have to do revert instead of just removing bad code
# 




